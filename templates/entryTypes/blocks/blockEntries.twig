{# >>> Comp Defaults #}
{% set compDefaults = {
  data: {
    name: 'c-blockEntries',
    block: null,
  },
  classes: {
    root: 'container grid grid-cols-12 gap-4 md:gap-8',
    custom: '',
  },
} %}

{# >>> Merge data / classes / variants (optional) #}
{% set props = {
  data: data is defined and data is iterable ? compDefaults.data | merge(data) : compDefaults.data,
  classes: classes is defined and classes is iterable ? compDefaults.classes | merge(classes) : compDefaults.classes,
} %}

{% if props.data.block.entryList ??? null %}
  <div class="{{ props.data.name }} {{ props.classes.root }}  {{ props.classes.custom }}">
    {# Headline #}
    {{ include('_components/text/textBlockHeadline.twig', {
      data: {
        headline: props.data.block.textTitle ??? null,
        text: props.data.block.textDescription ??? null,
      },
      classes: {
        custom: ''
      }
    }, withContext = false) }}

    {% set viewMode = props.data.block.entriesViewMode %}

    {% if viewMode == 'list'%}

      {# Entry List #}
      {{ include('_components/list/listEntries.twig', {
        data: {
          slides: props.data.block.entryList ??? null,
        },
        classes: {
          custom: 'col-span-12 mt-8'
        }
      }, withContext = false) }}

    {% elseif viewMode == 'slider' %}

      {# Entry Slider #}
      {{ include('_components/slider/sliderEntries.twig', {
        data: {
          slides: props.data.block.entryList ??? null,
        },
        classes: {
          custom: 'col-span-12 md:col-span-9 md:col-start-3 mt-8'
        }
      }, withContext = false) }}

    {% elseif viewMode == 'grid' %}

      {# Entry Grid #}
      {{ include('_components/grid/gridEntries.twig', {
        data: {
          slides: props.data.block.entryList ??? null,
        },
        classes: {
          custom: 'col-span-10 col-start-2 md:col-span-12 md:col-start-1 mt-8'
        }
      }, withContext = false) }}

    {% endif %}
  </div>
{% endif %}
